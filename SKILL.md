---
name: heicha-micro-motion-video
description: "当用户上传海报图或提供海报文字内容，并希望生成 Seedance 2.5 的 3–5 秒极简治愈系微动态视频提示词时使用。只允许黑色涂鸦小人运动，锁定主体、文字、背景、构图和镜头；不用于复杂剧情视频。"
---

# 喜茶式微动态视频提示词

## 目标

把用户上传的海报图或文字内容，整理成一段可直接粘贴给 `Seedance 2.5` 的图生视频提示词。默认输出 3–5 秒、可自然循环的极简治愈系微动态海报：海报中的真实主体、手写文案、背景、排版、留白、投影和整体构图保持静止，仅允许黑色手绘涂鸦小人做幅度很小的动作。

如果宿主明确提供视频生成能力且用户要求“直接生成视频”，先完成下面的提示词，再调用视频生成能力；如果用户只要提示词，就只交付提示词。没有实际生成结果时，不要声称视频已经生成。

## 输入判断

- 有 1 张上传海报图：把它视为唯一的画面基准，逐项读取主体、文案、线稿小人、投影、背景、布局和留白。
- 有多张图片且哪张是海报图不明确：先让用户指定；除非用户明确要求，否则不要混合不同海报的元素。
- 只有文字：把文字当作一张“待建立的静态海报设计说明”，先明确单一主体、白底、下置构图、黑色手绘小人和一句中文手写文案，再规定这张静态构图在视频中保持不变。若目标平台只接受图生视频，提醒用户先提供或生成海报图，不能假称已有原图。
- 用户给出新的文案或动作：只有在用户明确要求修改时才覆盖海报中的对应内容；其余静止元素仍然锁定。
- 用户指定时长或模型版本：遵守指定值；未指定时长默认 3–5 秒。首行只标注实际目标模型，不写未经确认的“降级”或多个版本。

## 生成规则

将下列规则整合成一段完整提示词，不要让用户再自行拼接。提示词首行标明 `Seedance 2.5`、图生视频、3–5 秒和“可循环”。

### 1. 静止要素：逐帧锁定

将上传海报图中的主体（甜品、产品或其他单一真实主体）、手写文案、背景、排版布局、留白比例、投影和整体构图定义为静止基准，要求全程逐帧保持一致：

- 主体不晃动、不形变、不融化、不缩放、不旋转，不发生位置偏移或材质变化；
- 文案文字不跳动、不闪烁、不重绘、不逐字出现、不改字、不变形、不位移；
- 背景、投影和留白区域完全静态，不新增光效、渐变闪烁、纹理、道具、人物或装饰；
- 不改变原图的裁切、景别、排版比例、色彩关系和视觉中心；
- 若为文字模式，先建立一张明确的白底静态海报，再把上述静止约束施加于该构图。

### 2. 唯一运动来源：黑色涂鸦小人

- 只有画面中已经存在的黑色手绘涂鸦小人可以运动；小人的数量、位置、线条风格和纯黑颜色保持不变。
- 动作必须承接原图中小人的既有姿态，并根据画面实际选择少量动作：轻微浇水、爬梯子、推车搬运、蹲下检查或测量、坐在主体边缘晃腿、拿小勺子舀取、擦拭、张望、记录等。
- 动作要与主体功能和小人所在位置有逻辑关系；如果原图中没有对应道具，不要运动中凭空新增道具。不要新增小人，不要让小人消失，不要让小人变成真实人物。
- 每个小人只做一个很小的连续动作，彼此可以有简单协作，例如两人合力推车或一人观察、一人操作；动作节奏缓慢、克制、容易循环。

### 3. 运动质感与循环

- 使用手绘定格动画（stop-motion / frame-by-frame）的质朴颗粒感，线稿可以有极轻微的逐帧抖动和不完全平滑感，但不能破坏可识别性。
- 避免 3D 插值感、丝滑运动、夸张弹跳、卡通形变和大幅位移；运动幅度小，像静态海报轻轻活了过来。
- 首帧和末帧回到同一构图与近似同一姿态，动作相位自然闭合；不要用突然冻结、闪白、跳切或重置来制造循环。

### 4. 镜头锁定

使用完全固定的 `fixed/static shot`，全程单一机位：不推拉（no zoom）、不摇移（no pan/tilt）、不旋转（no rotation）、不切换景别、不改变镜头角度、不改变裁切。镜头运动不是可用的动画来源。

## 固定负面约束

把以下内容放在提示词末尾，作为明确的 negative prompt 或“不要出现”清单：

不要主体晃动、变形、融化、缩放或位置偏移；不要文字位移、重绘、错字、乱码或闪烁；不要背景变化、光效扫过、渐变闪烁或投影移动；不要镜头运动、切镜、变焦、摇移或旋转；不要新增或消失的元素、小人、道具和装饰；不要给小人上色；不要小人变成真实人物；不要动作幅度夸张、卡通弹跳、丝滑 3D 插值或大幅位移；不要闪烁、重影、跳帧、拖影、随机纹理、局部重绘和其他 AI 视频瑕疵。

## 交付格式

默认只输出以下两部分，保持简洁：

1. `【Seedance 2.5 图生视频提示词】`：一段完整、可直接复制的提示词，包含时长、循环、静止要素锁定、唯一运动来源、运动质感、镜头锁定和负面约束。
2. `【素材使用】`：有图时说明“以上传海报图作为逐帧静止基准”；纯文字时说明这是文字模式，并提醒图生视频平台需要先提供海报图。

不要额外生成多个互相冲突的版本。用户要求修改时，只改指定的小人动作或节奏，并继续保留全部静止锁定、固定镜头、纯黑线稿和循环约束。
